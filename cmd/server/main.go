// Placeholder entrypoint for the HTTP API server.
// Full wiring (config, logging, router, handlers) is implemented in later phases.
package main

import (
	"log"
)

func main() {
	log.Println("rag-server: placeholder — server wiring lands in a later phase")
}
